import numpy as np

#排序：排序是暂时的，需要赋值储存起来
array = np.array([1,3,5,2,9,6])
array_sorted = np.sort(array)
print(array_sorted)

arr = np.array(['banana', 'cherry', 'apple'])
print(np.sort(arr))


arr = np.array([[3, 2, 4], [5, 0, 1]])
print(np.sort(arr))